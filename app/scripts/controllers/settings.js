'use strict';

/**
 * @ngdoc function
 * @name talkApp.controller:SettingsCtrl
 * @description
 * # SettingsCtrl
 * Controller of the talkApp
 */
angular.module('talkApp')
  .controller('SettingsCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
